spring:
  application:
    name: @pom.artifactId@
  datasource:
    url: jdbc:mysql://localhost:3306/wu-security?useSSL=false&allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
 # jackson时间格式化
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  http:
    encoding:
      force: true
      charset: utf-8
      enabled: true
  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
  security:
    filter:
      order: 3
server:
  port: 8401

eureka:
 instance:
    statusPageUrlPath: /actuator/info
    healthCheckUrlPath: /actuator/health
        # docker 部署开启
    #prefer-ip-address: true
    #ip-address: 127.0.0.1
 client:
   serviceUrl:
     defaultZone: http://localhost:8321/eureka/

management:
  endpoints:
    web:
      exposure:
        include: '*'

